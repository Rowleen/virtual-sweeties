import styles from "styles/components/footer.module.sass";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className={styles.wrapper}>
        Desarrollado con 💚 por Julián Peñaranda
      </div>
    </footer>
  );
};

export default Footer;
